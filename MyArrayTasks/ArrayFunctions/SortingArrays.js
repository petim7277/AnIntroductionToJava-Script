//Write a function sortArray that takes an array of numbers as input and returns a new array with the
// elements sorted in ascending order. Use an array method to perform the sorting.

let listOfScores = [34,56,21,30,17,92,44];
//let sortedArray = [];

let sortArray = function sortingArray(anArray ){
    let temp = 0;

    anArray.forEach((number)=>{

    })

}
console.log(sortArray);