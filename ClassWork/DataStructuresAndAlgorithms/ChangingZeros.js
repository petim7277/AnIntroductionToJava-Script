/*input = [[0,5],[2,3]]
output = [[0,0],[2,3]]
pseudocode .
============

 1. loop through the array
 2. write a condition that if a  number  at a particular index is equal to zero the number should be resigned with 0
 3. then return the array
 */
 let changingZeros = function (numberArray){
     let index = 0;
     let count = 0;
     for ( index = 0; index < numberArray.length ; index++) {
         for ( count = 0; count < numberArray.length ; count++) {
         }
     }

 }
let numArray = [[0,5],[2,3]];
 console.log(changingZeros(numArray));